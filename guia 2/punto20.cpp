#include<iostream>
using namespace std;
main()
{char op, sop;
int med,l1,l2,l3,l4,l5,l6,l7,l8,v1,v2,v3,v4,p1,p2,p3;

  cout<<"l longitud"<<endl;
  cout<<"v volumen"<<endl;
  cout<<"p peso"<<endl;
  
  cout<<"ingrese el tipo de medida que desea convertir";
  cin>>op;
  cout<<"ingrese el tipo de conversion que desea realizar ";
  cin>>sop;
  cout<<"ingrese la medida que desea convertir";
  cin>>med;
  
  if (sop=l1);
    {l1=med * 25.4;
    cout<<"la medida es "<<l1<<endl;
	}
else if( sop=='l2')
    {l2=med * 0.91;
    cout<<"la medida es "<<l2<<endl;
	}
else if( sop=='l3')
    {l3=med * 1.60;
    cout<<"la medida es "<<l3<<endl;
	}
else if( sop=='l4')
    {l4=med * 6.45;
    cout<<"la medida es "<<l4<<endl;
	}
else	if( sop=='l5')
    {l5=med * 0.09;
    cout<<"la medida es "<<l5<<endl;
	}
else 	if( sop=='l6')
    {l6=med * 0.83;
    cout<<"la medida es "<<l6<<endl;
	} 
else	if( sop=='l7')
    {l7=med * 0.40;
    cout<<"la medida es "<<l7<<endl;
	}
else	if( sop=='l8')
    {l8=med * 2.59;
    cout<<"la medida es "<<l8<<endl;
	}
else ( sop=='v1');
    {v1=med * 0.02;
    cout<<"la medida es "<<v1<<endl;
	}
else	if( sop=='v2')
    {v2=med * 0.76;
    cout<<"la medida es "<<v2<<endl;
	}
else	if( sop=='v3')
    {v3=med * 0.56;
    cout<<"la medida es "<<v3<<endl;
	}
else	if( sop=='v4')
    {v4=med * 4.54;
    cout<<"la medida es "<<v4<<endl;
	}
	else  ( sop=='p1')
    {p1=med * 28.35;
    cout<<"la medida es "<<p1<<endl;
	}
else	if ( sop=='p2')
    {p2=med * 0.45;
    cout<<"la medida es "<<p2<<endl;
	}
else	if( sop=='p3')
    {p3=med * 1.01;
    cout<<"la medida es "<<p2<<endl;
	}
system("pause");
}
